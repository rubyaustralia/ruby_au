Someone, hopefully you, requested we send you a link to change your password:

<%= edit_profile_password_url(token: @user.token) %>

If you didn't request this, ignore this email. Your password has not been changed.
