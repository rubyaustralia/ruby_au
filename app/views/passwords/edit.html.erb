<h2 class="rb-grey80 pl3">Change your password</h2>
<p>Choose a new password below.</p>

<%= form_for :change_password,
    url: profile_password_path(token: params[:token]),
    method: :put do |form| %>

  <div class="password-field">
    <%= form.label :password %>
    <%= form.password_field :password %>
  </div>

  <div class="submit-field">
    <%= form.submit 'Update password', class: "anon-pro b h2 w4 pt1 pb1 pl4 pr4 tc b--solid bw175 b--rb-purple rb-purple rb-white-hover rb-purple-focus bg-rb-purple-hover shadow-4 no-underline" %>
  </div>
<% end %>
